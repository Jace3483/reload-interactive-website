phases:
preBuild:
commands:
        # Optional: remove old cache to prevent npm errors
- rm -rf node_modules package-lock.json
        # Fresh install
- npm install
build:
commands:
- npm run build
artifacts:
    # Deploy everything inside the build folder
baseDirectory: build
files:
      - '**/*'
cache:
paths:
- node_modules/**/*